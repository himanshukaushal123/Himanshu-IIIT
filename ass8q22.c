#include<stdio.h>
int main()
{
    int i, n, x,z=1, s=0;
    printf("Give n: ");
    scanf("%d", &n);
    printf("Give %d numbers: \n", n);
    for(i=1; i<=n; i++)
    {
        scanf("%d", &x);
        
    }
    printf("OUTPUT: %d\n", s);
}